import { Box, Typography } from '@mui/material';

export const LoginPage = () => {
  return (
    <Box>
      <Typography variant="h2" fontWeight="400">
        Test
      </Typography>
    </Box>
  );
};
